plugin {
    hyprfocus {
        enabled = true

        keyboard_focus_animation = shrink

        shrink {
            shrink_percentage = 0.99

            in_bezier = linear
            out_bezier = linear

            in_speed = 0.5
            out_speed = 1
        }
    }
}
